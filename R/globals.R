globals <- c(
  "document", "text", "clean_text", "sentiment", "permalink", "V1", "V2",
  "select", "summarise", "tags", "div", "updateTextInput", "req",
  "reactive", ":=", "aes", "element_text", ".data", "words", "reorder",
  "plot_date", "n"
)

globalVariables(globals)
