# Add a re-sizeable text area to take notes in!

Add a re-sizeable text area to take notes in!

## Usage

``` r
ls_create_note_area()
```

## Value

a movable shiny text box

## Examples

``` r
if (FALSE) { # \dontrun{
ui <- shiny::fluidPage(
  ls_create_note_area()
)
} # }
```
